{{>header}}
{{>navbar}}
<table class="table table-bordered">
    <thead>
        <tr>
            <th>#</th>
            <th>name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {{#unless hasUser}}
        <tr>
            <td colspan="3">no data yet</td>
        </tr>
        {{else}}
        {{#each users}}
        <tr>
            <td>{{this._id}}</td>
            <td>{{this.name}}</td>
            <td>
                <a href="/single/{{this._id}}" class="btn btn-primary mx-2">Show</a>
                <a href="/edit/{{this._id}}" class="btn btn-warning mx-2">Edit</a>
                <a href="/del/{{this._id}}" class="btn btn-danger mx-2">Delete</a>
                                <a href="/addAddress/{{this._id}}" class="btn btn-success mx-2">add address</a>

            </td>
        </tr>
        {{/each}}
        {{/unless}}
    </tbody>
</table>
{{>footer}}