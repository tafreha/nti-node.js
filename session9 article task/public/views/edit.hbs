{{>header}}
{{>navbar}}
{{#unless article}}
<div class="alert alert-danger"> article not found
</div>
{{else}}
<form method="post">
	<div class="mb-3">
		<input  name="title"
class="form-control" value="{{article.title}}">
</div>
<div class="mb-3">
	<textarea  name="content"  row=" 12" class="form-control" value="{{article.content}}">
		</textarea>
</div>
<div class="mb-3">
            <button class="btn btn-primary">Edit</button>
			</div>

</form>
{{/unless}}
{{>footer}}
